/**
 * Set to true to temporarily allow access to /admin and /api/admin without login.
 * Set back to false when you want to re-enable admin authentication.
 */
export const ADMIN_AUTH_DISABLED = true;
